var config = {
  user : "root",
  password : "root", // leave this blank for windows machines
  port: 8080, //MAMP still not working, using XAMPP with port 8080
  host: "localhost",
  database: "db_flashback", //database connecting to

  kidsmode : false
}


module.exports = config;
